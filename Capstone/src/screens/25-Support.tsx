import React from 'react'
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, TextInput, View } from 'react-native'
import { MaterialCommunityIcons } from '@expo/vector-icons'
import { Text } from '../components/AppText'
import { createSupportTicket, fetchMySupportTickets, fetchSupportMessages, sendSupportReply, SupportMessage, SupportTicket } from '../lib/support'
import { colors } from '../theme/tokens'

export const SupportScreen = ({ onBack }: { onBack: () => void }) => {
  const [tickets, setTickets] = React.useState<SupportTicket[]>([])
  const [category, setCategory] = React.useState('booking')
  const [subject, setSubject] = React.useState('')
  const [description, setDescription] = React.useState('')
  const [message, setMessage] = React.useState('')
  const [loading, setLoading] = React.useState(true)
  const [sending, setSending] = React.useState(false)
  const [selected, setSelected] = React.useState<SupportTicket>()
  const [messages, setMessages] = React.useState<SupportMessage[]>([])
  const [reply, setReply] = React.useState('')
  const load = React.useCallback(async () => { const result = await fetchMySupportTickets(); setTickets(result.data); setMessage(result.message || ''); setLoading(false) }, [])
  React.useEffect(() => { void load() }, [load])

  async function submit() {
    if (!subject.trim() || !description.trim() || sending) return
    setSending(true); setMessage('')
    const result = await createSupportTicket({ category, subject, description })
    setSending(false)
    if (!result.ok) setMessage(result.message || 'Unable to create ticket.')
    else { setSubject(''); setDescription(''); setMessage('Your concern was sent to MULTIVENT Customer Service.'); await load() }
  }

  async function openTicket(ticket: SupportTicket) { setSelected(ticket); const result = await fetchSupportMessages(ticket.id); setMessages(result.data); if (result.message) setMessage(result.message) }
  async function replyToTicket() { if (!selected || !reply.trim()) return; setSending(true); const result = await sendSupportReply(selected.id, reply); setSending(false); if (!result.ok) setMessage(result.message || 'Unable to reply.'); else { setReply(''); await openTicket(selected) } }

  return <View style={styles.screen}>
    <View style={styles.header}><Pressable onPress={onBack} style={styles.back}><MaterialCommunityIcons name="arrow-left" color={colors.primaryDark} size={22}/></Pressable><View><Text style={styles.brand}>MULTIVENT</Text><Text style={styles.title}>Customer Support</Text></View><View style={styles.spacer}/></View>
    <ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled">
      <Text style={styles.heading}>How can we help?</Text><Text style={styles.copy}>Report account, booking, payment, or platform concerns. Service-quality feedback remains part of event reviews.</Text>
      <View style={styles.card}><Text style={styles.label}>Concern category</Text><View style={styles.categories}>{['account','booking','payment','system','technical','other'].map((item) => <Pressable key={item} onPress={() => setCategory(item)} style={[styles.category, category === item && styles.categoryActive]}><Text style={[styles.categoryText, category === item && styles.categoryTextActive]}>{item}</Text></Pressable>)}</View><Text style={styles.label}>Subject</Text><TextInput style={styles.input} value={subject} onChangeText={setSubject} placeholder="Brief summary" placeholderTextColor={colors.textMuted}/><Text style={styles.label}>Description</Text><TextInput multiline style={[styles.input,styles.textarea]} value={description} onChangeText={setDescription} placeholder="Tell us what happened and what you expected." placeholderTextColor={colors.textMuted}/>{message ? <Text style={styles.message}>{message}</Text> : null}<Pressable disabled={sending || !subject.trim() || !description.trim()} onPress={() => void submit()} style={[styles.submit,(sending || !subject.trim() || !description.trim()) && styles.disabled]}>{sending ? <ActivityIndicator color="white"/> : <Text style={styles.submitText}>Submit ticket</Text>}</Pressable></View>
      <Text style={styles.section}>Your tickets</Text>{loading ? <ActivityIndicator color={colors.primary}/> : tickets.length === 0 ? <Text style={styles.empty}>No support tickets yet.</Text> : tickets.map((ticket) => <Pressable key={ticket.id} onPress={() => void openTicket(ticket)} style={styles.ticket}><View><Text style={styles.ticketId}>MV-{String(ticket.ticketNumber).padStart(6,'0')} · {ticket.category.toUpperCase()}</Text><Text style={styles.ticketTitle}>{ticket.subject}</Text><Text style={styles.ticketDate}>{new Date(ticket.createdAt).toLocaleDateString('en-PH')}</Text></View><Text style={styles.status}>{ticket.status.replaceAll('_',' ')}</Text></Pressable>)}
      {selected ? <View style={styles.thread}><View style={styles.threadHeader}><Text style={styles.ticketTitle}>{selected.subject}</Text><Pressable onPress={() => setSelected(undefined)}><MaterialCommunityIcons name="close" color={colors.primaryDark} size={20}/></Pressable></View>{messages.length === 0 ? <Text style={styles.empty}>No replies yet.</Text> : messages.map((item) => <View key={item.id} style={[styles.bubble,item.mine && styles.bubbleMine]}><Text style={styles.bubbleAuthor}>{item.mine ? 'You' : 'MULTIVENT Support'}</Text><Text style={styles.bubbleText}>{item.body}</Text></View>)}{!['closed'].includes(selected.status) ? <><TextInput multiline style={[styles.input,styles.reply]} value={reply} onChangeText={setReply} placeholder="Reply to support" placeholderTextColor={colors.textMuted}/><Pressable disabled={sending || !reply.trim()} onPress={() => void replyToTicket()} style={[styles.submit,(sending || !reply.trim()) && styles.disabled]}><Text style={styles.submitText}>Send reply</Text></Pressable></> : null}</View> : null}
    </ScrollView>
  </View>
}

const styles = StyleSheet.create({
  back:{alignItems:'center',borderColor:colors.border,borderRadius:10,borderWidth:1,height:40,justifyContent:'center',width:40},brand:{color:colors.primary,fontFamily:'Inter_700Bold',fontSize:10,letterSpacing:1.4,textAlign:'center'},bubble:{alignSelf:'flex-start',backgroundColor:colors.background,borderRadius:10,maxWidth:'88%',padding:10},bubbleAuthor:{color:colors.primary,fontFamily:'Inter_700Bold',fontSize:9},bubbleMine:{alignSelf:'flex-end',backgroundColor:'#F4E8EB'},bubbleText:{color:colors.textPrimary,fontFamily:'Inter_400Regular',fontSize:12,lineHeight:18,marginTop:4},card:{backgroundColor:colors.surfaceElevated,borderColor:colors.border,borderRadius:16,borderWidth:1,padding:18},categories:{flexDirection:'row',flexWrap:'wrap',gap:7,marginBottom:16},category:{borderColor:colors.border,borderRadius:20,borderWidth:1,paddingHorizontal:11,paddingVertical:7},categoryActive:{backgroundColor:colors.primary,borderColor:colors.primary},categoryText:{color:colors.textSecondary,fontFamily:'Inter_500Medium',fontSize:11,textTransform:'capitalize'},categoryTextActive:{color:'white'},content:{padding:18,paddingBottom:50},copy:{color:colors.textSecondary,fontFamily:'Inter_400Regular',fontSize:13,lineHeight:20,marginBottom:20},disabled:{opacity:.5},empty:{color:colors.textMuted,fontFamily:'Inter_400Regular',fontSize:13,textAlign:'center'},header:{alignItems:'center',backgroundColor:colors.surfaceElevated,borderBottomColor:colors.border,borderBottomWidth:1,flexDirection:'row',justifyContent:'space-between',padding:14},heading:{color:colors.textPrimary,fontFamily:'Inter_700Bold',fontSize:24,marginBottom:7},input:{backgroundColor:colors.background,borderColor:colors.border,borderRadius:10,borderWidth:1,color:colors.textPrimary,fontFamily:'Inter_400Regular',fontSize:14,marginBottom:15,minHeight:46,paddingHorizontal:12,paddingVertical:10},label:{color:colors.textPrimary,fontFamily:'Inter_600SemiBold',fontSize:12,marginBottom:7},message:{color:colors.primaryDark,fontFamily:'Inter_500Medium',fontSize:12,marginBottom:12},reply:{marginTop:12,minHeight:75,textAlignVertical:'top'},screen:{backgroundColor:colors.backgroundSecondary,flex:1},section:{color:colors.textPrimary,fontFamily:'Inter_700Bold',fontSize:18,marginBottom:12,marginTop:26},spacer:{width:40},status:{backgroundColor:'#F4E8EB',borderRadius:20,color:colors.primaryDark,fontFamily:'Inter_600SemiBold',fontSize:10,overflow:'hidden',paddingHorizontal:10,paddingVertical:6,textTransform:'capitalize'},submit:{alignItems:'center',backgroundColor:colors.primary,borderRadius:11,justifyContent:'center',minHeight:49},submitText:{color:'white',fontFamily:'Inter_700Bold',fontSize:14},textarea:{minHeight:115,textAlignVertical:'top'},thread:{backgroundColor:colors.surfaceElevated,borderColor:colors.border,borderRadius:15,borderWidth:1,gap:8,marginTop:16,padding:14},threadHeader:{alignItems:'center',flexDirection:'row',justifyContent:'space-between',marginBottom:4},ticket:{alignItems:'center',backgroundColor:colors.surfaceElevated,borderColor:colors.border,borderRadius:13,borderWidth:1,flexDirection:'row',justifyContent:'space-between',marginBottom:9,padding:14},ticketDate:{color:colors.textMuted,fontFamily:'Inter_400Regular',fontSize:10,marginTop:4},ticketId:{color:colors.primary,fontFamily:'Inter_700Bold',fontSize:9,letterSpacing:.5},ticketTitle:{color:colors.textPrimary,fontFamily:'Inter_600SemiBold',fontSize:13,marginTop:5},title:{color:colors.textPrimary,fontFamily:'Inter_600SemiBold',fontSize:15,textAlign:'center'},
})
